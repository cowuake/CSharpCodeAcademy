@using Library.MVC.Models.BookGenres
@model CreateEditBookGenreViewModel
@{
    ViewData["Title"] = "Add new genre";
}

<h1>Add new genre</h1>

@*<form asp-action="Create" method="post" action="@Url.Action("Create")">*@
<form asp-action="Create">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row form-group">
            <label asp-for=Name class="col-sm-4 control-label text-sm-right"></label>
        <div class="col-sm-8 ">
            <input required asp-for=Name class="form-control"/>
            <span asp-validation-for=Name class="text-danger"></span>
        </div>
    </div>
    
    <hr />
    <div class="row">
        <div class="col text-right">
            <a class="btn btn-warning" href="@Url.Action("Index")">
                Cancel
            </a>
            <button type="submit" class="btn btn-success">
                Insert
            </button>
        </div>
    </div>
</form>