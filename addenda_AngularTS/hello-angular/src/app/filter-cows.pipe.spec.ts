import { FilterCowsPipe } from './filter-cows.pipe';

describe('FilterCowsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCowsPipe();
    expect(pipe).toBeTruthy();
  });
});
